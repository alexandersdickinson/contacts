<%@contacts.each() do |contact|%>
	<h2><li>
		<a href='/<%= contact.id()%>'><%= "#{contact.last_name()}, #{contact.first_name()}"%></a>
	</li></h2>
<%end%>

<a href='/new' class='btn btn-primary'>New</a>